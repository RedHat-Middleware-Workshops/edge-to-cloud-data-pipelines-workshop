This folder is intended to be used to provision DevSpaces.

Make sure that ZIP files are in sync with the last version of the sources (zipped folder)
 -> DevSpaces will download the ZIP containing the sources.